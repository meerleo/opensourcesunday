# Hugo build output (public/ is intentionally tracked — it's served directly
# by gotwebd on the server, so the built site ships with the repo).
resources/_gen/
.hugo_build.lock

# Hugo modules cache (local override)
/hugo_stats.json

# OS cruft
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Env files
.env
.env.local
